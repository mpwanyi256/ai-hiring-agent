import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com', 'msrspatwjkmyhgqucxuh.supabase.co', 'unsplash.com'],
  },
};

export default nextConfig;
